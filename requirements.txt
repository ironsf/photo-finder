openpyxl
Pillow
transformers

# torch (CPU-only) — install separately to avoid a huge CUDA download:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# deep-translator — only needed if TRANSLATE_TO_ENGLISH = True in config.py (off by default):
#   pip install deep-translator

# playwright — only needed if you add "bing" to SEARCH_PROVIDERS in config.py (off by default):
#   pip install playwright
#   playwright install chromium
