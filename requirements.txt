openpyxl
Pillow
transformers

# torch (CPU-only) — install separately to avoid a huge CUDA download:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# playwright — only needed if you re-enable USE_SCRAPE_SEARCH in config.py (off by default):
#   pip install playwright
#   playwright install chromium
